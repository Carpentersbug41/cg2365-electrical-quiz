import PhotosynthesisBuilder from './components/PhotosynthesisBuilder';

export default function App() {
  return (
    <div className="min-h-screen bg-sky-50 flex items-center justify-center p-4 font-sans">
      <PhotosynthesisBuilder />
    </div>
  );
}
