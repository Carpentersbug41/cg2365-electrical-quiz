# Google Gemini API Configuration
GEMINI_API_KEY=your_api_key_here

# AI Model Configuration
# Primary model for lesson generation (Phases 1-9)
# Available models: gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash-exp, gemini-3-flash-preview, gemini-3-pro
# Use gemini-3-flash-preview for fast, cost-effective generation
GEMINI_MODEL=gemini-3-flash

# Phase 10 Model (lesson improvements and scoring)
# Optional: If not set, uses GEMINI_MODEL
# Recommended: gemini-3-pro for best reasoning (higher cost but better improvements)
# - Gemini 3 Pro: Best-in-class reasoning (91.9% GPQA, 1501 Elo global #1)
# - Better issue detection, smarter fixes, higher quality scores (98-100 range)
# - Only 3 LLM calls per Phase 10 run (scoring, planning, rewriting)
PHASE10_MODEL=gemini-3-pro

# Phase 10 Debug Artifacts
# Enable to write full run bundles (prompts, outputs, scores, validation, diffs) to disk
# Default: 1 in development, 0 in production
PHASE10_DEBUG_ARTIFACTS=1
# Output path for debug artifacts (relative to project root)
PHASE10_DEBUG_PATH=reports/phase10_runs

# Phase 10-13 Verbose Debug Logging
# Enable verbose terminal output showing detailed pipeline execution
# Useful for debugging and understanding the Phase 10-13 pipeline
DEBUG_PHASE10=false
# Show full prompts and LLM responses (can be very long)
DEBUG_PHASE10_PROMPTS=false
# Show timing information for each phase (enabled automatically when DEBUG_PHASE10=true)
DEBUG_PHASE10_TIMING=true
