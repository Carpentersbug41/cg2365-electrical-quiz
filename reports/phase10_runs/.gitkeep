# Phase 10 Debug Artifacts

This directory contains comprehensive debug artifacts for every Phase 10 run.

Each run is saved in its own folder with all artifacts plus a combined markdown file.

## Structure

```
phase10_runs/
  - 203-3A1__2026-02-07T12-42-04-044Z__rewrite__gemini-3-flash-preview/
    - 00_input_lesson.json
    - 01_score_before.json
    - 02_prompt_rewrite.json
    - 03_output_rewrite.txt
    - 10_output_lesson.json
    - 11_score_after.json
    - 14_patches_parsed.json
    - 16_validation.json
    - 17_diff.txt
    - INDEX.json
    - COMBINED.md  <- All information in one file
```

**Folder name format:** `<lessonId>__<timestamp>__<strategy>__<model>`

## Configuration

Enable/disable artifact recording via `.env`:

```bash
PHASE10_DEBUG_ARTIFACTS=1  # 1 = enabled, 0 = disabled
PHASE10_DEBUG_PATH=reports/phase10_runs
```

Default: **Always enabled** (hardcoded to true).

## Using COMBINED.md

The `COMBINED.md` file contains:
- Run summary (scores, delta, validation status)
- All prompts and outputs embedded
- Validation results
- Diffs
- Complete scoring breakdowns

**Benefits:**
- Open in any markdown viewer
- Copy the entire run in one file
- Search through all data at once
- Easy to share or archive

## Purpose

These artifacts enable complete replayable visibility into:
- Prompts sent to the model
- Raw model outputs (unparsed)
- Scoring breakdowns (before/after)
- Validation results (per validator)
- Human-readable diffs

Use these to debug:
- Prompt effectiveness
- Model behavior patterns
- Parsing failures
- Validation rejections
- Score regressions
- Patch application issues
